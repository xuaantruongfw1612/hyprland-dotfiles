# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Commands and Apps to be executed at launch
# --- Workspaces configuration by Xuan Truong ---

$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

$wallDIR=$HOME/Pictures/wallpapers
$lock = $scriptsDir/LockScreen.sh
$SwwwRandom = $UserScripts/WallpaperAutoChange.sh
$livewallpaper="$HOME/Pictures/wallpapers/dark-knight-frostbound.1920x1080.mp4"

# wallpaper stuff 
exec-once = swww-daemon --format xrgb # hình nền thường thì cmt line exec-once = swww-daemon --format...
#exec-once = mpvpaper '*' -o "load-scripts=no no-audio --loop" $livewallpaper

# wallpaper random
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes

# Startup
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Polkit (Polkit Gnome / KDE)
exec-once = $scriptsDir/Polkit.sh

# starup apps
exec-once = nm-applet --indicator
exec-once = swaync
#exec-once = ags
exec-once = blueman-applet
#exec-once = rog-control-center
exec-once = waybar
exec-once = qs # quickshell AGS Desktop Overview alternative

# clipboard manager
exec-once = CLIPHIST_MAX_ITEMS=5000 wl-paste --type text --watch cliphist store
exec-once = CLIPHIST_MAX_ITEMS=100 wl-paste --type image --watch cliphist store

# Rainbow borders
exec-once = $UserScripts/RainbowBorders.sh

# Starting hypridle to start hyprlock
exec-once = hypridle

# Network auto-switcher
exec-once = $UserScripts/Network-switcher.sh

# Battery
exec-once = $scriptsDir/Battery.sh

# Sounds start
exec-once = sleep 2 && paplay /home/truong/.config/swaync/Sounds/CypherUltEnemyCast.mp3

# Key
exec-once = gnome-keyring-daemon --start --components=secrets

# Here are list of features available but disabled by default
#exec-once = swww-daemon --format xrgb && swww img $HOME/Pictures/wallpapers/MangaWallpaperGIF.gif  # persistent wallpaper

#gnome polkit for nixos
#exec-once = $scriptsDir/Polkit-NixOS.sh

# xdg-desktop-portal-hyprland (should be auto starting. However, you can force to start)
#exec-once = $scriptsDir/PortalHyprland.sh
